---
type Props = {
	date: string;
};

const { date } = Astro.props;
---

<a
	href={`/date/${encodeURI(new Date(date).getFullYear().toString())}`}
	target="_self"
	class="px-2.5 py-1 rounded-lg text-xs bg-neutral text-neutral-content"
>
	{date}
</a>
